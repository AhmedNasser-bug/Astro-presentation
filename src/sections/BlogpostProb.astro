---
import BundleComparison from '../components/BundleComparison.jsx';
import { Scale } from 'lucide-react';
---

<section class="relative py-24 px-6 border-b border-white/5 bg-void-950">
    <div class="max-w-5xl mx-auto">
        
        <!-- Section Header -->
        <div class="text-center mb-16 space-y-4">
            <div className="inline-flex items-center gap-2 text-astro mb-4">
                <Scale className="w-6 h-6" />
                <span class="text-sm font-mono font-bold tracking-widest uppercase">Payload Analysis</span>
            </div>
            
            <h2 class="text-4xl md:text-5xl font-bold text-white tracking-tight">
                The <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-red-500">Reality Check</span>
            </h2>
            
            <p class="text-lg text-slate-400 max-w-2xl mx-auto">
                We often reach for complex tools to solve simple problems. 
                Here is the actual cost of "Hello World" in modern frameworks.
            </p>
        </div>

        <!-- The Interactive Island -->
        <div class="p-8 rounded-2xl bg-void-900 border border-white/10 shadow-2xl">
            <BundleComparison client:visible />
        </div>

        <!-- Footer Note -->
        <div class="mt-8 flex justify-center gap-8 text-sm text-slate-500 font-mono">
            <div class="flex items-center gap-2">
                <div class="w-2 h-2 rounded-full bg-astro"></div>
                <span>Ideal Target</span>
            </div>
            <div class="flex items-center gap-2">
                <div class="w-2 h-2 rounded-full bg-red-500"></div>
                <span>Hydration Overhead</span>
            </div>
        </div>

    </div>
</section>