---
import { AlertTriangle, Clock, HardDrive, Cpu } from 'lucide-react';
---

<section class="relative py-24 px-6 border-b border-white/5 bg-void-950 overflow-hidden">
    <!-- Red/Warning Ambient Glow -->
    <div class="absolute top-1/2 left-0 w-1/3 h-1/2 bg-red-900/10 blur-[100px] -translate-y-1/2 pointer-events-none"></div>

    <div class="max-w-6xl mx-auto relative z-10">
        
        <!-- Header -->
        <div class="text-center max-w-3xl mx-auto mb-16">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-red-500/10 border border-red-500/20 text-red-400 text-xs font-mono mb-6">
                <AlertTriangle size={12} />
                CRITICAL WARNING: TBT HIGH
            </div>
            
            <h2 class="text-4xl md:text-5xl font-bold text-white tracking-tight mb-6">
                The JavaScript <span class="text-red-500">Tax</span>
            </h2>
            
            <p class="text-lg text-slate-400 leading-relaxed">
                In traditional Single Page Applications (SPAs), the browser must hydrate 
                <span class="text-white font-semibold">everything</span> just to show 
                <span class="text-white font-semibold">anything</span>.
            </p>
        </div>

        <!-- The "Monolith" Visualization Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            
            <!-- Card 1: Network -->
            <div class="group p-6 rounded-2xl bg-void-900 border border-white/5 hover:border-red-500/30 transition-colors duration-500">
                <div class="w-12 h-12 bg-red-500/10 rounded-lg flex items-center justify-center mb-4 text-red-500 group-hover:scale-110 transition-transform">
                    <HardDrive size={24} />
                </div>
                <h3 class="text-xl font-bold text-white mb-2">Network Bottleneck</h3>
                <p class="text-slate-400 text-sm">
                    Downloading 500KB+ of JavaScript bundles just to render static text and images.
                </p>
            </div>

            <!-- Card 2: CPU -->
            <div class="group p-6 rounded-2xl bg-void-900 border border-white/5 hover:border-red-500/30 transition-colors duration-500">
                <div class="w-12 h-12 bg-red-500/10 rounded-lg flex items-center justify-center mb-4 text-red-500 group-hover:scale-110 transition-transform">
                    <Cpu size={24} />
                </div>
                <h3 class="text-xl font-bold text-white mb-2">Main Thread Blocked</h3>
                <p class="text-slate-400 text-sm">
                    Hydration locks up the CPU. Users tap buttons, but nothing happens until the script finishes.
                </p>
            </div>

            <!-- Card 3: Time -->
            <div class="group p-6 rounded-2xl bg-void-900 border border-white/5 hover:border-red-500/30 transition-colors duration-500">
                <div class="w-12 h-12 bg-red-500/10 rounded-lg flex items-center justify-center mb-4 text-red-500 group-hover:scale-110 transition-transform">
                    <Clock size={24} />
                </div>
                <h3 class="text-xl font-bold text-white mb-2">Slow TTI</h3>
                <p class="text-slate-400 text-sm">
                    Time To Interactive suffers. Mobile users on 4G networks wait seconds for a responsive page.
                </p>
            </div>

        </div>
    </div>
</section>