---
import PrinciplesGrid from '../components/PrinciplesGrid.jsx';
import { ShieldAlert } from 'lucide-react';
---

<section class="relative py-24 px-6 border-b border-white/5 bg-void-950 overflow-hidden">
    <!-- Ambient Background Elements -->
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-slate-900/40 via-void-950 to-void-950 pointer-events-none"></div>
    
    <div class="max-w-6xl mx-auto relative z-10">
        
        <!-- Header -->
        <div class="text-center max-w-3xl mx-auto mb-20">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-orange-500/10 border border-orange-500/20 text-orange-400 text-xs font-mono mb-6">
                <ShieldAlert size={12} />
                ROOT CAUSE ANALYSIS
            </div>
            
            <h2 class="text-4xl md:text-5xl font-bold text-white tracking-tight mb-6">
                Forgotten <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 to-red-500">Fundamentals</span>
            </h2>
            
            <p class="text-lg text-slate-400 leading-relaxed">
                The modern web performance crisis isn't a tooling problem. <br class="hidden md:block" />
                It's a violation of core software engineering principles.
            </p>
        </div>

        <!-- The Grid -->
        <PrinciplesGrid client:visible />

        <!-- Footer / Insight -->
        <div class="mt-16 text-center">
            <p class="text-sm font-mono text-slate-500">
                // These principles guided the design of the Astro compiler.
            </p>
        </div>

    </div>
</section>