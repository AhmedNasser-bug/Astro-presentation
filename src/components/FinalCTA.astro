
<div id="launch-pad" class="w-full max-w-4xl mx-auto text-center p-12 rounded-3xl bg-void-950 border border-white/10 relative overflow-hidden group">
    
    <!-- Dynamic Background -->
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-astro/20 via-void-950 to-void-950 opacity-0 group-hover:opacity-100 transition-opacity duration-1000"></div>
    
    <div class="relative z-10 flex flex-col items-center gap-8">
        
        <div class="w-20 h-20 rounded-full bg-white flex items-center justify-center shadow-[0_0_50px_rgba(255,255,255,0.3)] animate-pulse-slow">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="black" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 7-7 7 7"/><path d="M12 19V5"/></svg>
        </div>

        <h2 class="text-4xl md:text-6xl font-black text-white tracking-tight">
            Ready for <span class="text-transparent bg-clip-text bg-gradient-to-r from-orange-400 via-red-500 to-purple-600">Liftoff?</span>
        </h2>

        <p class="text-xl text-slate-400 max-w-2xl">
            Build faster websites with less JavaScript. <br/>
            Start your next project with Astro today.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 mt-4">
            <a href="https://astro.build" target="_blank" class="px-8 py-4 rounded-full bg-white text-void-950 font-bold text-lg hover:scale-105 transition-transform shadow-[0_0_30px_rgba(255,255,255,0.3)] flex items-center gap-2">
                Get Started
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </a>
            <a href="https://docs.astro.build" target="_blank" class="px-8 py-4 rounded-full bg-white/5 border border-white/10 text-white font-bold text-lg hover:bg-white/10 transition-colors">
                Read Docs
            </a>
        </div>

        <div class="mt-8 text-xs font-mono text-slate-600">
            Powered by Vanilla HTML & CSS (0kb Runtime)
        </div>

    </div>
</div>

<script>
    // Vanilla JS interaction
    const pad = document.getElementById('launch-pad');
    if (pad) {
        pad.addEventListener('mousemove', (e) => {
            const rect = pad.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            pad.style.setProperty('--x', `${x}px`);
            pad.style.setProperty('--y', `${y}px`);
        });
    }
</script>

<style>
    #launch-pad::before {
        content: '';
        position: absolute;
        left: var(--x, 50%);
        top: var(--y, 50%);
        width: 400px;
        height: 400px;
        background: radial-gradient(circle, rgba(255,255,255,0.03) 0%, transparent 70%);
        transform: translate(-50%, -50%);
        pointer-events: none;
        transition: opacity 0.5s;
    }
</style>
